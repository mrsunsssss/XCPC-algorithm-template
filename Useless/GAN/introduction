生成对抗网络 GAN 是一种基于博弈论思想的生成模型，由 生成器（Generator, G） 和 判别器（Discriminator, D） 组成.
二者通过相互对抗的方式训练，最终生成器能够生成逼真的数据。GAN的核心原理可以分为以下几个方面：

生成器（G）：
输入：随机噪声 z（通常服从正态分布或均匀分布）。
输出：生成的假数据（例如图像、音频等）。
目标：生成尽可能逼真的数据，使得判别器无法分辨真假。

判别器（D）：
输入：一批真实数据和生成器生成的假数据。
输出：判断输入数据是真实（值接近1）还是假的（值接近0）的概率。概率即为真图像的概率
目标：准确区分真实数据和生成数据。


3. 训练过程
GAN的训练是生成器和判别器之间的交替优化过程：
训练判别器（D）：
输入一批真实数据和生成器生成的假数据。
计算判别器的损失，更新判别器的参数，使其能够更好地区分真实数据和生成数据。
训练生成器（G）：
输入随机噪声 z ，生成假数据 𝐺(z) .
计算生成器的损失，更新生成器的参数，使其生成的假数据能更好地欺骗判别器。

